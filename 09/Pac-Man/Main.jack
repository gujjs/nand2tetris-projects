class Main {
    function void main() {

        var Game game;

        let game = Game.new();
        
        while (true) {
            do game.status_update(Keyboard.keyPressed());
        }

        return;
    }
}
